machine:
  ruby:
    version: 2.0.0-p353

deployment:
  staging:
    branch: multitenant
    heroku:
      appname: uruut-multitenant

database:
  override:
    - mv config/database.ci.yml config/database.yml
    - bundle exec rake db:create db:schema:load --trace