if ( $("#contact_list").val() == "" ){
  var emails = "<%= @emails %>";
  $("#contact_list").val( $("#contact_list").val() + emails );
}
else{
  <% unless @emails.blank? %>
  var emails = ",<%= @emails %>";
  $("#contact_list").val( $("#contact_list").val() + emails );
  <% end %>
}
