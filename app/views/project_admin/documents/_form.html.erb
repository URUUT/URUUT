<%= form_for(@project_admin_document) do |f| %>
  <% if @project_admin_document.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_admin_document.errors.count, "error") %> prohibited this project_admin_document from being saved:</h2>

      <ul>
      <% @project_admin_document.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :document %><br />
    <%= f.text_field :document %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br />
    <%= f.number_field :project_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
