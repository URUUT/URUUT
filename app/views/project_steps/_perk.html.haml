= f.fields_for :perks, :child_index => "test" do |p|
  .perk
    %div
      = p.label :name
      = p.text_field :name, :required => true

    %div
      = p.label :amount
      = p.text_field :amount, :required => true

    %div
      = p.label :description
      = p.text_area :description, :required => true

    %a.remove{:href => "#"} Remove X
